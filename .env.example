# ============================================================
# Planning Poker — Environment variables
# Copy this file to .env and adjust values as needed.
# ============================================================

# Host port to expose the app on (container always listens on 3000)
APP_PORT=3000

# Room TTL in seconds (default: 1 hour = 3600)
ROOM_EXPIRATION_TIME=3600
